#项目名称
spring:
  application:
    name: user-center
  datasource:
    driver-class-name: com.mysql.jdbc.Driver
    url: jdbc:mysql://localhost:3306/wang-an
    username: root
    password: 200151
#session失效时间
  session:
    timeout: 86400

server:
  port: 8080
  servlet:
    context-path: /api

# mybatis-plus的配置
mybatis-plus:
  configuration:
    map-underscore-to-camel-case: false
  global-config:
    db-config:
      logic-delete-field: flag
      logic-delete-value: 1 #逻辑已删除，默认值为1；
      logic-not-delete-value: 0 #逻辑未删除，默认值为0